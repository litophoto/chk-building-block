import ComponentBlock from "./features/component-block/Index";

function App() {
  return (
    // <div>
    //   {users.map((user: any) => (
    //     <div key={user.id}>{user.name}</div>
    //   ))}
    // </div>
    <>
      <ComponentBlock />
    </>
  );
}

export default App;
